CREATE TABLE place  (
ID varchar(15),
name  varchar(25),
adress  varchar(50)
);

CREATE TABLE employees (
ID varchar(15),
first_name varchar(25),
last_name  varchar(25),
department varchar(15),
email  varchar(50)
);
